# Mafia Telegram Bot (aiogram 3.9.0)

Минимальный бот для игры в Мафию в Telegram-группах.

## Возможности

- Создание лобби в групповом чате
- Подключение и выход игроков
- Назначение ролей (Мафия/Мирный)
- Отправка роли каждому игроку в личные сообщения

## Стек

- Python 3.11+
- aiogram 3.9.0

## Быстрый старт

1. Создай бота через @BotFather и получи токен.
2. В корне проекта создай `.env` на основе `.env.example`.
3. Установи зависимости:

```bash
pip install -r requirements.txt
```

4. Запусти бота:

```bash
python main.py
```

## Команды бота

- `/start` - помощь
- `/create` - создать лобби (только в группе)
- `/join` - войти в лобби
- `/leave` - выйти из лобби
- `/lobby` - показать текущих игроков
- `/begin` - начать игру (только создатель лобби)
- `/close` - закрыть лобби (только создатель лобби)

## Важно

Перед раздачей ролей каждый игрок должен написать боту в личку `/start`, иначе бот не сможет отправить ему роль.

## Деплой на GitHub

Если репозиторий еще не создан, выполни:

```bash
git init
git add .
git commit -m "Initial mafia bot with aiogram 3.9.0"
git branch -M main
git remote add origin https://github.com/<your-username>/<your-repo>.git
git push -u origin main
```

Если репозиторий уже подключен:

```bash
git add .
git commit -m "Add mafia bot"
git push
```

## Дальше для прод-деплоя

GitHub хранит код, но не запускает бота постоянно. Для 24/7 запуска используй VPS, Railway, Render или другой хостинг.
